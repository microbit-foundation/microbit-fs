/**
 * @type {import('./types').PrettierConfig}
 */
const config = {
  singleQuote: true,
  arrowParens: 'always',
  bracketSpacing: true,
  trailingComma: 'es5',
  printWidth: 80,
};

module.exports = config;
